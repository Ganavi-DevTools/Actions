{
  "name": "Proj",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest", 
    "build": "mkdir -p build && cp index.js build/",
    "lint": "eslint ."
  },
  "devDependencies": {
    "jest": "^29.0.0"  
}
